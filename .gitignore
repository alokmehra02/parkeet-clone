__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.DS_Store

# ── Secrets — NEVER commit these ──────────────────────────────
.env

# ── Virtual environment ────────────────────────────────────────
.venv/
venv/

# ── App runtime data (contains API key & session DB) ──────────
# ~/.meetassist/ lives outside the repo, but guard against stray symlinks
sessions.db
config.json
